{
  "name": "codex-task-control",
  "version": "0.13.1",
  "private": true,
  "description": "A quota-aware controller with parallel fan-out, lightweight lifecycle receipts, on-demand timing diagnostics, review gates, and visible-task-only delegation.",
  "type": "module",
  "scripts": {
    "check": "node --check skill/codex-task-control/scripts/task-control.mjs",
    "test": "node --test tests/task-control.test.mjs skill/codex-task-control/scripts/task-control.unit.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
