{
  "name": "codex-task-control",
  "version": "0.12.0",
  "private": true,
  "description": "A quota-aware controller that enforces parallel fan-out, queues messages, reviews, and archives visible Codex tasks while forbidding internal subagents.",
  "type": "module",
  "scripts": {
    "check": "node --check skill/codex-task-control/scripts/task-control.mjs",
    "test": "node --test tests/task-control.test.mjs skill/codex-task-control/scripts/task-control.unit.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
