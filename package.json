{
  "name": "codex-task-control",
  "version": "0.23.0",
  "private": true,
  "description": "A quota-aware visible-task controller with adaptive health-aware one-shot watchdogs, bidirectional queued notifications, progressive direct-parent context, bounded same-task repairs, parallel fan-out, safe handoff, and on-demand diagnostics.",
  "type": "module",
  "scripts": {
    "check": "node --check skill/codex-task-control/scripts/task-control.mjs",
    "test": "node --test tests/task-control.test.mjs skill/codex-task-control/scripts/task-control.unit.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
