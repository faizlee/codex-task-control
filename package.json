{
  "name": "codex-task-control",
  "version": "0.2.0",
  "private": true,
  "description": "A quota-aware gate that forbids internal subagents and routes work to visible Codex tasks.",
  "type": "module",
  "scripts": {
    "check": "node --check skill/codex-task-control/scripts/task-control.mjs",
    "test": "node --test tests/task-control.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
