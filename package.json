{
  "name": "codex-task-control",
  "version": "0.17.0",
  "private": true,
  "description": "A quota-aware visible-task controller with parallel fan-out, immutable conversation checkpoints, progressive preload, safe controller handoff, and on-demand diagnostics.",
  "type": "module",
  "scripts": {
    "check": "node --check skill/codex-task-control/scripts/task-control.mjs",
    "test": "node --test tests/task-control.test.mjs skill/codex-task-control/scripts/task-control.unit.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
