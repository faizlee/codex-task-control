interface:
  display_name: "Codex Task Control"
  short_description: "Receipt-safe parallel control and Chinese diagnostics"
  default_prompt: "Use $codex-task-control to fan out valuable visible tasks, confirm host receipts before lifecycle changes, recover stuck attempts, and generate Chinese delivery diagnostics on demand."
