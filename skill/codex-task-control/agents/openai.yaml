interface:
  display_name: "Codex Task Control"
  short_description: "Route visible tasks with safe heartbeat commits"
  default_prompt: "Use $codex-task-control to bind implementation work to staged contracts and manage visible-task heartbeats through prepare, create-new, confirm, switch, and stale self-cleanup without advancing the ledger before Codex App success."
