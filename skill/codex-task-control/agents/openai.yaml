interface:
  display_name: "Codex Task Control"
  short_description: "Queue messages and stop quota-burning task loops"
  default_prompt: "Use $codex-task-control to bind visible tasks to schema-v2 contracts, defer ordinary messages until the target is idle, ingest first-class failure events, detect stalls and retry fuses, enforce controller handoff, close incidents with a delivery report, and finalize terminal heartbeat cleanup before more project work."
