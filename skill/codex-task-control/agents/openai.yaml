interface:
  display_name: "Codex Task Control"
  short_description: "Stop stalled visible tasks before quota burn"
  default_prompt: "Use $codex-task-control to bind visible tasks to schema-v2 contracts, ingest first-class failure events, detect stalls and retry fuses, enforce controller handoff, and close incidents with a user summary plus delivery report."
