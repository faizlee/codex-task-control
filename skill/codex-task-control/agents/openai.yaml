interface:
  display_name: "Codex Task Control"
  short_description: "Name, review, and archive visible Codex tasks"
  default_prompt: "Use $codex-task-control to route work only to registered visible tasks, keep sidebar titles synchronized, and archive terminal tasks without deleting ledger history."
