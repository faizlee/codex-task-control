interface:
  display_name: "Codex Task Control"
  short_description: "Fan out visible tasks without quota-burning loops"
  default_prompt: "Use $codex-task-control to plan a schema-v1 parallel batch, enforce safe multi-task fan-out and WIP, bind every visible task to its candidate and implementation contract, queue ordinary messages, detect failures and stalls, review outcomes, and finalize the shared controller heartbeat before more project work."
