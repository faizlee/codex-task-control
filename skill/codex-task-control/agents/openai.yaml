interface:
  display_name: "Codex Task Control"
  short_description: "Visible tasks with protected controller continuity"
  default_prompt: "Use $codex-task-control to protect long-lived controller identity, recover accidental terminal closure through a visible successor, and coordinate auditable task lifecycle."
