interface:
  display_name: "Codex Task Control"
  short_description: "Parallel visible-task control with on-demand diagnostics"
  default_prompt: "Use $codex-task-control to fan out safe visible tasks, preserve lightweight lifecycle receipts, and generate an on-demand timing and quota-proxy HTML report when requested."
