interface:
  display_name: "Codex Task Control"
  short_description: "Route visible tasks with auditable delivery reports"
  default_prompt: "Use $codex-task-control to bind visible implementation tasks to staged contracts, validate result artifacts, review attempts, and refresh the controller delivery HTML."
