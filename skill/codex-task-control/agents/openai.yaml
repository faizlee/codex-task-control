interface:
  display_name: "Codex Task Control"
  short_description: "Stop stalled visible tasks before quota burn"
  default_prompt: "Use $codex-task-control to bind visible tasks to schema-v2 contracts, ingest first-class failure events, detect stalls and retry fuses, enforce controller handoff, close incidents with a delivery report, and finalize terminal heartbeat cleanup before more project work."
